"use client" import { Card, CardContent, CardDescription, CardHeader, CardTitle } from "@/components/ui/card" interface ProductInfoProps { name: string price: number description: string } export default function ProductInfo({ name, price, description }: ProductInfoProps) { return ( <Card className="w-full"> <CardHeader> <CardTitle className="text-3xl font-bold">{name}</CardTitle> <CardDescription className="text-2xl text-primary font-semibold mt-2"> {price.toLocaleString()}원 </CardDescription> </CardHeader> <CardContent> <h3 className="text-lg font-semibold mb-2">상품 설명</h3> <p className="text-gray-700 leading-relaxed whitespace-pre-wrap">{description}</p> </CardContent> </Card> ) }